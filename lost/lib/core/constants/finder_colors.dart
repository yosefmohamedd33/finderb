import 'package:flutter/material.dart';

/// Custom Colors specific to the Finder app design
class FinderColors {
  // Primary dark blue color from inside pages
  static const Color primaryBlue = Color(0xFF0A3D91);
  static const Color primaryBrown = Color(0xFF0A3D91); // Kept for compatibility but points to blue
  static const Color lightBlue = Color(0xFF2E5A8C);
  static const Color lightBrown = Color(0xFF2E5A8C); // Kept for compatibility but points to light blue
  static const Color darkBrown = Color(0xFF05204A); // Kept for compatibility but points to darker blue
  
  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  // Background
  static const Color background = Colors.white;
}
